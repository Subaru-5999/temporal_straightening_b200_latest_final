"""Test suite for the counterfactual-curvature-regularization feature.

Making `tests` a package keeps the repo root on `sys.path` under pytest's default import
mode, so both `models.*` and `tests.reference_impl` import cleanly.
"""
